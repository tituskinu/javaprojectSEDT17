package class10;

public class E1StringDemo {
    public static void main(String[] args) {
        //we have  created an object from a string
        // class and have stored Cat inside it.
        String obj=new String("Cat");
        String obj2="MY Dog"; //String obj=new String("Cat");   (same thing behind the move)
        int len=obj2.length();// calling length methode/function/action/behaviour
        System.out.println(len);



    }
}
